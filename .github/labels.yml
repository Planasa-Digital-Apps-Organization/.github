# Org-wide label catalog. Source of truth — propagated to every
# non-archived Planasa repo by .github/workflows/sync-labels.yml.
#
# Format: array of { name, color (6-hex, no #), description }.
# Description max 100 chars (GitHub API limit).
#
# When adding a label here, push to main and let the sync workflow run
# (or trigger it manually via workflow_dispatch).

- name: 'area:ai-behavior'
  color: 'fbca04'
  description: 'Touches .claude/**, CLAUDE.md, AGENTS.md, .github/** — shapes AI behavior / PR safeguards'
